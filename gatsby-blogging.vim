" Useful stuff for Gatsby blogging

" Create blog post:
" needs to get title as argument
" :command! CreateBlogPost strftime
"
" create blog post (get current date etc)
" snippets for yaml files, etc
